
export const WITHDRAWAL_RULES = {
  MIN_AMOUNT: 0.10,
  MAX_AMOUNT: 5.00,
  GAS_FEE: 0.03,
  MAX_WITHDRAWALS_24H: 2,
};

export const ADMIN_CONFIG = {
  REFERRAL_LOCKED: true, // Simulate admin lock for the referral balance
};
